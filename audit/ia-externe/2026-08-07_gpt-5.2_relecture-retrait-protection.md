SEVERITE: MAJEUR  
FICHIER:lib/services/security/folder_vault_service.dart:1183-1188 (méthode `removeVaultProtection`)  
DEFAUT: Le service peut lever `VaultLockedException` (session fermée) dans un chemin UI légitime où l’utilisateur vient d’entrer la passphrase, à cause d’une course entre `vault.isUnlocked()` (UI) et `_requireSession()` (service).  
SCENARIO: Ouvrir le drawer d’un coffre → “Retirer la protection” → la session est expirée/auto-lock se déclenche juste après le check `if (!vault.isUnlocked(folder.id))` mais avant l’appel `vault.removeVaultProtection(folder)` → `_requireSession(folder.id)` lève `VaultLockedException` → catch générique UI → snack “error …” au lieu de redemander le déverrouillage, l’action échoue alors que l’utilisateur est dans un flux attendu.  
CORRECTIF: Dans `_removeVaultProtection` (UI), catcher spécifiquement `VaultLockedException` et relancer `showUnlockVaultAdaptive` puis réessayer l’opération (ou déplacer la logique “demander unlock si nécessaire” dans le service avec un callback/erreur dédiée que l’UI traite en re-prompt).