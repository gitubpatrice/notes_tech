SEVERITE: CRITIQUE  
FICHIER:lib/data/db/notes_dao.dart:318-329  
DEFAUT: l’UPDATE écrit littéralement les clés `'title': ?title` et `'enc_v': ?encVersion` au lieu d’utiliser les variables, ce qui ne compile pas (ou casse l’exécution si “toléré” via dynamic).  
SCENARIO: `FolderVaultService._reprotectPlaintextNotes` ou `_migrateLegacyEncryptedNotes` appelle `replaceContentPayload(... title: '', encVersion: 2)` ⇒ `NotesDao.replaceContentPayload` est appelé ⇒ build échoue (erreur de syntaxe/typage Map) ou l’app crashe dès qu’on tente d’ouvrir un coffre (chemin PIN ou passphrase).  
CORRECTIF: remplacer par `'title': title, 'enc_v': encVersion` (et éventuellement ne pas inclure ces clés dans la Map quand les params sont `null` si vous voulez un UPDATE vraiment partiel).