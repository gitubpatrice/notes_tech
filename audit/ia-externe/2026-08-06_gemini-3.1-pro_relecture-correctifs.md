SEVERITE: MINEUR
FICHIER: lib/services/settings_service.dart:65
DEFAUT: Le commentaire affirme qu'un `switch` exhaustif garantit une erreur de compilation en cas d'oubli d'un mode de tri, mais le code utilise une `Map` (`_sortModeKeys[mode]!`) qui provoquera une exception au moment de l'exécution (Null check operator).
SCENARIO: Un développeur ajoute une valeur à l'enum `NoteSortMode` en se fiant au commentaire, la compilation réussit, mais l'appel à `setSortMode` avec ce nouveau mode lève un `TypeError` à l'exécution.
CORRECTIF: Remplacer la `Map` par un `switch` exhaustif dans une méthode, ou corriger le commentaire pour refléter la réalité du code.

SEVERITE: MAJEUR
FICHIER: lib/ui/screens/note_editor_screen.dart:503
DEFAUT: L'export utilise `fresh.isLocked` pour détecter si la note vient d'un coffre, mais cette condition est toujours fausse car `fresh` est la note éphémère déchiffrée en RAM (dont `encryptedContent` est `null`).
SCENARIO: Ouvrir une note de coffre dans l'éditeur, puis l'exporter. Le fichier généré n'aura ni le suffixe `[unlocked]` ni la mention d'origine dans le frontmatter, car `fresh.isLocked` renvoie systématiquement faux.
CORRECTIF: Utiliser `_wasLocked` au lieu de `fresh.isLocked` pour déterminer la valeur de `fromVault`.