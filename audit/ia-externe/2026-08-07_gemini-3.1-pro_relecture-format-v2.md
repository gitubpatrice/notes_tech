SEVERITE: CRITIQUE
FICHIER:lib/data/db/notes_dao.dart:316
DEFAUT: Erreur de syntaxe Dart (`?title` et `?encVersion` au lieu de `title` ou `if (title != null) ...`) qui empêche purement et simplement la compilation de l'application.
SCENARIO: La compilation échoue immédiatement à cause de la syntaxe invalide `?title` dans la map littérale, rendant l'application inutilisable.
CORRECTIF: Remplacer `'title': ?title` par `if (title != null) 'title': title` (et de même pour `encVersion`).