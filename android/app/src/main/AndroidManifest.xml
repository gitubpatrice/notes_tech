<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <!--
      Notes Tech — 100% local, zéro réseau.
      Aucune permission INTERNET n'est déclarée intentionnellement.
      Toute future fonctionnalité réseau devra être justifiée et auditable.

      Les déclarations `tools:node="remove"` ci-dessous neutralisent les
      permissions que des dépendances transitives essaient d'injecter
      (ex. `background_downloader` tiré par flutter_gemma) — l'app
      n'utilise PAS ces fonctions et ne doit JAMAIS demander ces droits.
      À auditer sur chaque release via `aapt dump permissions`.
    -->
    <uses-permission android:name="android.permission.INTERNET" tools:node="remove" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" tools:node="remove" />
    <uses-permission android:name="android.permission.WAKE_LOCK" tools:node="remove" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" tools:node="remove" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" tools:node="remove" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" tools:node="remove" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" tools:node="remove" />

    <!--
      v0.6 — Speech-to-Text Whisper on-device. Permission demandée à
      l'utilisateur lors du premier appui sur le bouton micro de l'éditeur.
      Aucune capture sans interaction explicite. Audio jamais persisté
      (cf. SttSession dans files_tech_voice).
    -->
    <uses-permission android:name="android.permission.RECORD_AUDIO" />

    <application
        android:label="Notes Tech"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher"
        android:allowBackup="false"
        android:fullBackupContent="false"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:networkSecurityConfig="@xml/network_security_config"
        android:usesCleartextTraffic="false">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <meta-data
                android:name="io.flutter.embedding.android.NormalTheme"
                android:resource="@style/NormalTheme" />
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />

        <!--
          flutter_gemma / MediaPipe : OpenCL natif pour accélération GPU.
          `required="false"` → app fonctionne sans (CPU fallback).
        -->
        <uses-native-library
            android:name="libOpenCL.so"
            android:required="false" />
        <uses-native-library
            android:name="libOpenCL-car.so"
            android:required="false" />
        <uses-native-library
            android:name="libOpenCL-pixel.so"
            android:required="false" />
    </application>

    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT" />
            <data android:mimeType="text/plain" />
        </intent>
        <!--
          v0.6 voice — déléguer le DL du modèle Whisper au navigateur
          système. Android 11+ exige cette déclaration <queries> pour
          que l'app puisse "voir" un browser et lui passer un Intent VIEW.
          On ne fait QUE déléguer — la permission INTERNET reste retirée,
          c'est le navigateur qui télécharge.
        -->
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <category android:name="android.intent.category.BROWSABLE" />
            <data android:scheme="https" />
        </intent>
    </queries>
</manifest>
